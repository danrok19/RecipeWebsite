import { FlexFilterPipe } from './flex-filter.pipe';

describe('FlexFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FlexFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
